Use stack to record the numbers, and pop them out when the operators occurs.

Pay attention to the division, round it to the nearest integer.

We need to change it to float then return it to integer.
